packages:
  - cvmfs cvmfs-fuse3

repos:
  - cernvm

postprocess:
  - |
    #!/usr/bin/env bash
    set -euo pipefail
    ln -s run/cvmfs /cvmfs
    echo 'd /run/cvmfs 0755 root root -' >> /usr/lib/tmpfiles.d/cvmfs.conf
