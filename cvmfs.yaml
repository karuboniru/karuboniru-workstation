packages:
  - cvmfs cvmfs-fuse3

repos:
  - cernvm

postprocess:
  - |
    #!/usr/bin/env bash
    echo 'd /cvmfs 0755 root root -' >> /usr/lib/tmpfiles.d/cvmfs.conf
    echo -e '[root]\ntransient = true' >> /usr/lib/ostree/prepare-root.conf
