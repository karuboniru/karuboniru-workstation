packages:
  - gaze 
  - gaze-gui
  - gaze-kde

repos:
  - gundulabs
  - dummy

postprocess:
  - |
    #!/usr/bin/env bash
    echo "Switch to gaze authentication profile"
    authselect select gaze with-silent-lastlog with-altfiles --force --nobackup
    systemctl enable gazed.service
